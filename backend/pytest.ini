[pytest]
markers =
    llm_required: marks tests that require a running LLM / FTAL harness (deselect with '-m "not llm_required"')
    real_pf: marks tests that need real PersonaForge functions (bypasses conftest PF mock)
