Flask>=3.1.0
gunicorn>=23.0.0
Flask-CORS>=6.0.0
Flask-Limiter>=4.0.0
# numpy/pandas floors MUST precede python-jobspy: pip otherwise backtracks to an
# old jobspy whose numpy sdist cannot build on Python 3.13+. Verified 2026-09-04.
numpy>=2.1
pandas>=2.2
spacy>=3.8.0
nltk>=3.9.1
python-dotenv>=1.2.2
SQLAlchemy>=2.0.36
Werkzeug>=3.1.0
pypdf>=4.0.0
python-docx>=1.1.0
requests>=2.31.0
httpx>=0.27
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.0.0
python-pptx>=0.6.21
openpyxl>=3.1.0
python-arango>=8.0.0  # NOTE: if import fails, run: pip uninstall arango && pip install --force-reinstall python-arango
python-jobspy>=0.40
markdownify>=0.14.1  # pin transitive dep from python-jobspy; CVE-2025-46656
PyJWT>=2.8.0
pytest>=7.4.0
moto[secretsmanager]>=5.0  # AWS mocking for the ISecretStore contract suite
azure-keyvault-secrets>=4.8  # azure arm of the ISecretStore contract suite
azure-identity>=1.15  # DefaultAzureCredential for the Key Vault adapter
qdrant-client>=1.7.0
stomp.py>=8.1.0
boto3>=1.34.0
# PostgreSQL / OpenSearch support (required when CLOUDLIFT_ENV=aws)
psycopg2-binary>=2.9.0
opensearch-py>=2.4.0

# PDF export (resume_export.py imports these unguarded inside export_resume_pdf)
reportlab>=4.0
pytest-asyncio>=0.23  # @pytest.mark.asyncio tests fail with "async def not natively supported" without it
pytest-timeout>=2.2  # per-test timeouts; a hung test otherwise consumes the whole CI budget
